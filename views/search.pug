extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching by #{searchingBy}
    .search__videos
        each item in dummy
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })